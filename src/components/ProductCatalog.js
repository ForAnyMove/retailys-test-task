// import ProductItem from "./ProductItem";

function ProductCatalog(props) {
    return (
      <ul>
          {/* {Array.map(el => <ProductItem text="hello" />)} */}
      </ul>
    );
  }
  
  export default ProductCatalog;